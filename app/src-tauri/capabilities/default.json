{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC and the native file-open dialog only.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
