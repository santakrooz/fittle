# Chip

Filter pill for narrowing a list (All · Lights · Stacks · Calib · Rejects). `on` marks the active filter with `accent-soft` and `accent-fg`.

- The consumer supplies the label and toggles `on`. Counts go in the label ("Rejects 373") when they help.
- Chips filter; they never trigger commands. Use `Button` for commands.
