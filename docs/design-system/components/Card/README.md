# Card

Bordered container for one topic in the inspector or a report (Target, Equipment, Conditions, Integration).

- The consumer supplies `title` (a noun, sentence case), an optional `badge` (one `Badge`) and the body, usually `Fields`.
- `radius-card`, `line` border, `surface` ground, no shadow. Cards stack with `space-12` gaps; never nest a card in a card.
