# Kbd

A keyboard hint beside an action or inside the search pill.

- The consumer supplies the key text: `⌘K`, `esc`, `↵ edit`, `Space`.
- `mono-sm` on `surface-2` with a heavier bottom border. Show the platform's modifier (⌘ on macOS, Ctrl elsewhere).
