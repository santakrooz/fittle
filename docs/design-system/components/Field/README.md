# Field

An uppercase `label` over a value, in a two-column `Fields` grid. The basic unit of every inspector card.

- The consumer supplies `label`, `value` and optionally `unit` (muted, after the value) and `derived` (adds the teal DERIVED tag for computed values).
- Values use tabular figures. Never put a value without its unit.
