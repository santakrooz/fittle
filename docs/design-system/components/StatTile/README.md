# StatTile

KPI tile: one big number and its label, in rows of three to five across a report or session header.

- The consumer supplies `value`, optional `unit` (muted, smaller) and `label` (uppercase).
- `display-number` with tabular figures. Use tiles only when the figures are the point of the view.
