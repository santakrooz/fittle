# Tabs

Pill tabs across the top of the inspector: Overview · Header · Histogram · Stars.

- The consumer supplies `tabs`, `value` and `onChange`. Three to five tabs; one word each.
- The selected tab sits on `surface-2` in `text`; others are `text-muted`.
